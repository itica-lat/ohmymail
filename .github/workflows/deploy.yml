name: Deploy

on:
  push:
    branches: [main]

concurrency:
  group: deploy-mail
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: self-hosted
    timeout-minutes: 10
    steps:
      - name: Deploy
        run: bash /home/hermes/deploy/ohmy.sh mail